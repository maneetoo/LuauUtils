[package]
name = "maneetoo/task"
description = "Wraps a Lua coroutine with lifecycle management, priority, timeout awareness, automatic retry logic, and structured result/error propagation."
version = "0.1.0"
license = "MIT"
authors = ["maneetoo"]
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
[dependencies]
