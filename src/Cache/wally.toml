[package]
name = "maneetoo/cache"
description = "A simple full-typed LRU (Least Recently Used) cache with a pre-defined maximum size."
version = "0.1.0"
license = "MIT"
authors = ["maneetoo"]
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
[dependencies]
